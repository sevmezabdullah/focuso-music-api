<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Focuso Admin Paneli</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .sidebar {
            min-height: 100vh;
            background-color: #343a40;
        }
        .sidebar a {
            color: #fff;
            text-decoration: none;
            padding: 10px 15px;
            display: block;
        }
        .sidebar a:hover {
            background-color: #495057;
        }
        .sidebar .active {
            background-color: #0d6efd;
        }
        .main-content {
            padding: 20px;
        }
        .premium-badge {
            background-color: #ffd700;
            color: #000;
            padding: 2px 8px;
            border-radius: 12px;
            font-size: 0.8em;
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-2 sidebar p-0">
                <div class="d-flex flex-column">
                    <div class="p-3 text-white">
                        <h4>Focuso Admin</h4>
                    </div>
                    <a href="/admin" class="{{#if isHome}}active{{/if}}">
                        <i class="fas fa-home me-2"></i> Ana Sayfa
                    </a>
                    <a href="/admin/tracks" class="{{#if isTracks}}active{{/if}}">
                        <i class="fas fa-music me-2"></i> Müzikler
                    </a>
                    <a href="/admin/playlists" class="{{#if isPlaylists}}active{{/if}}">
                        <i class="fas fa-list me-2"></i> Çalma Listeleri
                    </a>
                    <a href="/admin/categories" class="{{#if isCategories}}active{{/if}}">
                        <i class="fas fa-tags me-2"></i> Kategoriler
                    </a>
                    <a href="/admin/users" class="{{#if isUsers}}active{{/if}}">
                        <i class="fas fa-users me-2"></i> Kullanıcılar
                    </a>
                </div>
            </div>

            <!-- Main Content -->
            <div class="col-md-10 main-content">
                {{{body}}}
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    {{#if message}}
    <script>
        Swal.fire({
            title: '{{message.title}}',
            text: '{{message.text}}',
            icon: '{{message.type}}',
            timer: 3000
        });
    </script>
    {{/if}}
</body>
</html>